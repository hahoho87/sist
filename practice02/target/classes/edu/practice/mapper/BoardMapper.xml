<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.practice.mapper.BoardMapper">

	<select id="getList" resultType="edu.practice.domain.BoardVO">
		<!-- 부등호를 쓰기 위해 CDATA를 사용 -->
	<![CDATA[
	SELECT * FROM t_board WHERE bno > 0
	]]>
	</select>
	
	<!-- insert만 처리되고 생성된 PK값을 알 필요가 없는 경우 -->
	<insert id="insert">
		INSERT INTO t_board (bno, title, content, writer)
		VALUES (t_board_seq.nextval, #{title}, #{content}, #{writer})
	</insert>

	<!-- insert가 실행되고 생성된 PK값을 알아야 하는 경우 -->
	<insert id="insertSelectKey">

		<selectKey keyProperty="bno" order="BEFORE" resultType="long">
			SELECT t_board_seq.nextval FROM dual
		</selectKey>

		INSERT INTO t_board (bno, title, content, writer)
		VALUES (#{bno}, #{title}, #{content}, #{writer})
	</insert>

	<!-- read -->
	<select id="read" resultType="edu.practice.domain.BoardVO">
		SELECT * FROM t_board WHERE bno = #{bno}
	</select>

	<!-- delete -->
	<delete id="delete">
		DELETE t_board where bno = #{bno}
	</delete>

	<!-- update -->
	<update id="update">
		UPDATE t_board
		SET title = #{title}, content = #{content}, 
			writer = #{writer}, updateDate = sysdate
		WHERE bno = #{bno}
	</update>
	
	<!-- getTotalCount -->
	<select id="getTotalCount" resultType="int">
		SELECT COUNT(*) FROM t_board WHERE bno > 0
	</select>

</mapper>